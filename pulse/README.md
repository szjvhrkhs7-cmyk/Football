# Пульс

Финансовое PWA для телефона и компьютера. На iPhone можно установить на экран «Домой» из Safari. Это веб-приложение с отдельным окном и офлайн-режимом, а не нативная iOS-сборка.

## Разделы

- **Обзор:** планируемый бюджет, остаток, все неоплаченные траты выбранного месяца, редактирование бюджета и категорий.
- **Кредиты:** название кредита, ежемесячный платёж с копейками, первая учитываемая дата, необязательная дата окончания, отметка закрытия. Сумма, оплачено и остаток показываются за выбранный месяц. Платежи 29–31-го числа переносятся на последний существующий день короткого месяца, сохраняя исходный день в следующих месяцах. Отметки оплаты независимы для каждого месяца.
- **Аналитика:** структура расходов и распределение по неделям.
- **Настройки:** аккаунт, облачная синхронизация, экспорт и импорт JSON.

Вкладка «Планы» удалена из нижней навигации. История всех трат, включая оплаченные, открывается из обзора кнопкой «Все траты».

Кредиты учитываются отдельно от расходных записей. Отметка оплаты не создаёт расход и не уменьшает бюджет повторно. Изменение размера платежа меняет расчёт для всех месяцев; закрытый кредит исключается из графика, но доступен в «Все кредиты». Это календарь обязательств, не банковский график погашения с расчётом процентов.

## Архитектура

- `index.html`, `styles.css`: готовая оболочка и адаптивный интерфейс. Бюджет и категории сразу находятся на главном экране; перестановки DOM и наблюдатели для исправления текста удалены.
- `app.js`: состояние, формы, навигация с сохранением позиции прокрутки, сохранение и синхронизация.
- `loans.js`: отдельные чистые правила валидации и календаря кредитов.
- Локальные данные сохраняются в прежнем `pulse-finance-v1`; старые копии без `loans` читаются с пустым списком кредитов. Экспорт, импорт и облачный JSON включают кредиты и отметки платежей.
- `sw.js`: кеш оболочки, ограниченный папкой приложения. Код и библиотека авторизации доступны офлайн; облачные запросы не кешируются. Ссылки подтверждения не сохраняются как отдельные страницы кеша. Обновление активируется после закрытия старых вкладок, чтобы не прерывать ввод в формах.
- `vendor/supabase-2.45.4.js`: прежняя фиксированная версия Supabase JS, поставляемая вместе с приложением вместо внешнего CDN. Источник: официальный npm-пакет `@supabase/supabase-js@2.45.4`, `dist/umd/supabase.js`; лицензия рядом.

Сервер: существующий Supabase Auth + Postgres (`pulse_finance_state`) + Realtime. В клиенте используется только публичный publishable key. RLS ограничивает строки условием `auth.uid() = user_id`; кредиты помещаются в существующий JSON, миграция таблицы не нужна.

Ограничения существующей синхронизации: один снимок состояния на аккаунт, сравнение по времени изменения; одновременные изменения разных устройств не объединяются по отдельным записям. Локальный профиль пока один на браузер. Для полноценного многопользовательского режима следующий отдельный этап: локальное хранилище по аккаунтам и разрешение конфликтов отдельных записей. После обновления нужно перезапустить приложение на всех устройствах: старый клиент не знает поля кредитов и может перезаписать облачное состояние без него.

## Регистрация

Клиент проверяет email и пароль, блокирует повторное нажатие во время запроса, сохраняет канонический адрес возврата без параметров сборки, показывает коды ошибок доставки и позволяет повторить подключение. Ссылка с ошибкой подтверждения открывает настройки с объяснением.

Проверено 21.09.2026 на действующем проекте: signup включён, email-провайдер включён, подтверждение email обязательно, социальные провайдеры выключены. RLS-политики чтения/записи проверены. Почтовая конфигурация требует отдельного доступа в панель Supabase; доставка реального письма не подтверждена. **Это обновление клиентского кода не подтверждает исправление серверной отправки писем.**

Для завершения проверки владельцу проекта нужно предоставить вход в панель Supabase; там проверяются SMTP, URL Configuration и ошибки Auth. Встроенный отправитель Supabase ограничивает адресатов членами команды проекта. Для произвольных адресатов нужен настроенный SMTP. Подтверждение email не отключается.

Официальные источники:
- https://supabase.com/docs/guides/auth/auth-smtp
- https://supabase.com/docs/guides/auth/passwords
- https://supabase.com/docs/guides/auth/rate-limits

## Запуск и проверка

```sh
cd pulse
npm ci
npm test
npm run check
npm start
```

Открыть `http://localhost:8765/`. Для публикации сборка не требуется: файлы отдаются статически. `jsdom` используется только для тестов, не загружается пользователю.

Тесты проверяют календарь, CRUD через DOM-события, оплату и отмену оплаты по месяцам, перезапуск, старые копии, безопасный вывод названий, сохранность обычных трат, обработку Auth-ошибок, синхронизацию и кеширование. DOM-тесты не заменяют проверку Safari, реальную доставку письма и синхронизацию двух авторизованных устройств.
