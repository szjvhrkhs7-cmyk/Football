{
  "name": "pulse-finance",
  "version": "1.1.0",
  "private": true,
  "scripts": {
    "test": "node --test tests/*.test.cjs",
    "check": "node --check app.js && node --check loans.js && node --check sw.js",
    "start": "python3 -m http.server 8765"
  },
  "devDependencies": { "jsdom": "26.1.0" }
}
